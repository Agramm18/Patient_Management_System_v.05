package app.controller.auth.account.admin.create;

public class CreateAdmin {
    public void LoacalAdmin() {
        System.out.println("\nWelcome you can now create a new local admin in the DB");
        System.out.println("Admin credentials: Username (Admin.Doe), Role, Rights/Permissions, Hashed PWSD\n");
    }
}
